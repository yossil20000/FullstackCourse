//export const getLenght1 = value => value.length;
/* export const isPalindrom = value => {
    const length = Math.floor(getLenght(value) / 2);
    const size = getLenght(value) -1;
    for (let index = 0; index < length; index++) {
        if(value[index] != value[size - index])
        return false;
    }
    return true;
} */
